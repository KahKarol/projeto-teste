name: Continuous Delivery

on: 
  push:
    branches:
      - main

jobs:
  deliver:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Simulando Delivery
        run: echo "Delivery ok"

  deploy:
    runs-on: ubuntu-latest
    needs: deliver
    steps:
      - name: Simulando Deploy
        run: echo "Deploy ok" ###### SOCORRRROOOOOO